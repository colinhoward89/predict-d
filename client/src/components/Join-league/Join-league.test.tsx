import React from 'react';
import ReactDOM from 'react-dom';
import JoinLeague from './Join-league';

it('It should mount', () => {
  const div = document.createElement('div');
  // ReactDOM.render(<JoinLeague />, div);
  ReactDOM.unmountComponentAtNode(div);
});